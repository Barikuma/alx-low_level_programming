#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: 0 if equal, < 0 if s1 < s2 and > 0 if s1 > s2
 */

int _strcmp(char *s1, char *s2)
{
	if (s1[i] > s2[i])
		return (15);
	else if (s1[i] < s2[i])
		return (-15);
	return (0);
}
